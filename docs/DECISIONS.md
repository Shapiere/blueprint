# Decisions

## Purpose

Decision log. Every entry uses the template below. This file owns the history of *why*; [CHANGELOG.md](../CHANGELOG.md) owns the history of *what changed*.

**Template:**

```text
### D<number> — <title>

- Date: YYYY-MM-DD
- Context: <situation and constraints>
- Decision: <what was chosen>
- Alternatives: <options considered and why they lost>
- Rationale: <the reason this decision serves the mission>
```

---

### D1 — Constitution committed to the repository (audit C1)

- Date: 2026-08-02
- Context: The audit found the bootstrap specification existed only in chat history, contradicting the repository-first rule. Future agents would have no constitution.
- Decision: The specification is committed as `docs/BOOTSTRAP_SPEC.md` (v1.1) and referenced from `AI_CONTEXT.md`.
- Alternatives: Distill into `AI_CONTEXT.md` only — rejected: loses the full operating rules; a summary cannot govern.
- Rationale: Repository-first is only enforceable when the rules are inside the repository.

### D2 — Secrets policy adopted (audit C2)

- Date: 2026-08-02
- Context: The environment holds live credentials (`~/.pi/agent/auth.json`); the definition of done requires pushing to a remote.
- Decision: `.gitignore` excludes auth/oauth/models/.env files; the constitution gains a Secrets Policy; `docs/SETUP.md` stores placeholders and re-entry instructions only.
- Alternatives: Snapshot configuration including secrets — rejected: credential leak on push.
- Rationale: A single bad commit would make the remote a credential dump; exclusion by policy and by tooling is the simplest defense.

### D3 — Setup inventory adopted as "describe, not snapshot" (audit C3)

- Date: 2026-08-02
- Context: The repository's mission is improving the Pi setup, but the original structure had no artifact describing the setup itself.
- Decision: `docs/SETUP.md` documents the environment (prereqs, provider, models, settings, packages, extensions, skills, templates, MCP) with `last-verified` dates and a restore procedure. Unknown facts are marked **Pending**.
- Alternatives: (a) Copy config files into the repository — rejected: config files drift, embed secrets, and duplicate the live source; (b) a sync script — rejected: adds tooling without proven value (Milestone 2 may revisit).
- Rationale: Description plus placeholders is reproducible without becoming a second source of truth for the configuration itself.

### D4 — Repository structure retained (audit M1)

- Date: 2026-08-02
- Context: The audit flagged tension between "no unnecessary folders" and the mandated `docs/` + `implementation/` split.
- Decision: Keep the split; the constitution now records the justification.
- Alternatives: Flatten `implementation/TODO.md` into `ROADMAP.md` — rejected: roadmap owns strategy, TODO owns actionable tasks; merging would blur ownership.
- Rationale: Two folders, each with a distinct owner file, is the minimum structure that keeps the ownership matrix clean.

### D5 — Documentation ownership matrix adopted (audit I1)

- Date: 2026-08-02
- Context: Five files naturally drift into "where are we now"; duplication was near-certain.
- Decision: The constitution includes a one-owner-per-fact matrix and the rule "reference, never restate."
- Alternatives: Accept duplication and reconcile at milestones — rejected: divergence is the failure mode this repository exists to prevent.
- Rationale: Ownership boundaries are the cheapest enforceable anti-duplication mechanism.

### D6 — Content contracts adopted (audit I2)

- Date: 2026-08-02
- Context: "Complete enough to serve as a solid foundation" was not a deterministic contract for unusual files.
- Decision: The constitution defines minimum content for `AI_CONTEXT.md`, `SUCCESS_CRITERIA.md`, `DECISIONS.md`, `PROJECT_STATE.md`, and `NEXT_SESSION.md`.
- Rationale: Deterministic bootstrap requires deterministic content requirements.

### D7 — Capture loop adopted (audit I3)

- Date: 2026-08-02
- Context: Setup changes happen far more often than milestone boundaries; status-only updates would let the repository go stale.
- Decision: Every setup change updates `docs/SETUP.md` and `CHANGELOG.md` in the same session; notable choices also update this log.
- Rationale: The repository's value is reflecting reality; staleness turns it into a liability.

### D8 — Anti-drift conventions adopted (audit I4)

- Date: 2026-08-02
- Context: For a configuration repository, the live environment is the ground truth and documentation can drift from it.
- Decision: `last-verified` dates on every `docs/SETUP.md` section; the cold-install test is a success criterion and Milestone 2 item.
- Rationale: Dates make drift visible; the cold-install test converts the inventory from prose into proof.

### D9 — Minor findings disposition (audit M2–M5)

- Date: 2026-08-02
- Context: Minor findings reviewed for maintainability value without added complexity.
- Decision: M2 adopted — `CONTRIBUTING.md` is explicitly agent-facing. M3 adopted — constitution gains an Amendment Process. M4 adopted — product referenced as "Pi". M5 adopted — no CI/linting added.
- Rationale: Each is a one-line or one-section change with no complexity cost; tooling (M5) remains rejected per the Never Overengineer principle.

### D10 — Audit archive location

- Date: 2026-08-02
- Context: The full audit lives outside this repository (`G:/blueprint-plan`); future agents cannot read chat history.
- Decision: The audit is not copied into the repository; its dispositions are recorded here (D1–D9) and the audit location is noted in `AI_CONTEXT.md`.
- Alternatives: Copy the full audit in — rejected: adds a large artifact whose conclusions are fully captured in the decision log; the constitution forbids documentation without clear value.
- Rationale: The decisions are the durable part; the analysis is archived on disk for reference.

### D11 — Remote repository visibility determined: public

- Date: 2026-08-02
- Context: Milestone 2 open question; the secrets policy's strictness depends on who can read the repository.
- Decision: Recorded here as resolved. The repository is **public**: an unauthenticated GitHub API request (`GET https://api.github.com/repos/Shapiere/blueprint`) returned repository metadata (a private repo would return `Not Found`).
- Alternatives: Assume visibility — rejected: verification was one request away.
- Rationale: Public visibility makes the secrets policy mandatory, not advisory; it remains in force unchanged.

### D12 — Cold-install validation approach (simulation over fabrication)

- Date: 2026-08-02
- Context: Milestone 2 requires executing the cold-install procedure, but no fresh machine is available. Results must never be fabricated.
- Decision: Perform the strongest practical validation: (a) a fresh-config simulation — a temporary agent directory containing the documented `models.json`, minimal `settings.json`, and the documented placeholder auth (`sk_9router`), run with `PI_CODING_AGENT_DIR`; (b) a checklist review of every remaining restore step against the live machine.
- Alternatives: (a) Destructive reinstall of pi on this machine — rejected: risks the working setup without adding evidence; (b) declare the criterion unvalidated without attempting — rejected: real evidence was obtainable.
- Result: Simulation passed (`FRESH_OK`, 2026-08-02): restore steps 4–5 verified end-to-end, including that the local router accepts the placeholder key. Steps 1–3 and 6–9 verified by checklist. Full fresh-machine execution and the interactive `/login` flow remain carried forward in `implementation/TODO.md`.
- Rationale: Honest partial validation with documented evidence beats either fabrication or blanket deferral.

### D13 — Milestone 3 candidate evaluations: no setup additions adopted

- Date: 2026-08-02
- Context: ROADMAP Milestone 3 candidates — additional packages/extensions, more MCP servers, new templates/skills — each require justification before adoption. No concrete workflow need was demonstrated for any of them.
- Decision: Evaluate each candidate against success criteria 4 (context economy) and 5 (secrets) and the demonstrated workflow; adopt none.
- Evaluations: (1) Packages/extensions — the installed set already covers identified needs (web access, subagents, MCP adapter, LSP feedback); remaining gallery candidates (todo overlays, security-audit suites, background-task managers, large skill packs) either duplicate capability, add credential burden, or inflate always-loaded context. (2) MCP servers — `chrome-devtools` covers browser workflows; filesystem/github/playwright candidates duplicate built-in tools or require new credentials. (3) Templates/skills — the existing three (`commit`, `review`, `explain`) cover the documented workflows; no real-usage gap was demonstrated.
- Alternatives: Adopt the most popular candidates (e.g., `rpiv-todo`, `piolium`) — rejected: no demonstrated need; contradicts practicality and criterion 4.
- Rationale: "Reject complexity without measurable value." The milestone's depth work is the evaluation itself, recorded here for future sessions to reference instead of re-running.

### D14 — Depth documentation folded into SETUP.md (no new files)

- Date: 2026-08-02
- Context: Milestone 3 depth analysis found operational and recovery knowledge undocumented: daily start sequence, health check, update commands, the `auth.json` `type`-field incident recovery, and a weak restore step 4 ("exact file: on the source machine").
- Decision: Extend `docs/SETUP.md` with an Operations section, a Troubleshooting table, a verified redacted `models.json` example, and a host path-resolution note. No new documents.
- Alternatives: New `docs/OPERATIONS.md` — rejected: would require an ownership-matrix amendment (constitution change) for content that fits SETUP.md's charter ("what is installed and how to run/rebuild it"), and risks duplication.
- Rationale: Depth without new structure; the ownership matrix stays untouched and the capture loop stays the single update path.

### D15 — Platform architecture adopted (constitution v1.2)

- Date: 2026-08-03
- Context: Milestone 5.5 delivered the AI Engineering Platform architecture; Milestone 6 begins actual platform implementation.
- Decision: Adopt `docs/ARCHITECTURE.md` as the governing architecture reference (layers, capability lifecycle, governance, integration architecture) and `capabilities/` as the platform asset tree with `capabilities/index.md` as the capability registry. Constitution amended to v1.2 (structure + ownership matrix).
- Alternatives: Defer structure change until Wave 2 — rejected: the registry and asset homes are prerequisites for Wave 1 integration itself.
- Rationale: Architecture before implementation; the registry is the single source of truth for capability status.

### D16 — External skills: reference, don't vendor (license gate)

- Date: 2026-08-03
- Context: anthropics doc skills (docx/pdf/pptx/xlsx) are source-available/proprietary; this repository is public (D11). Placing the clone in `~/.pi/agent/skills/` accidentally auto-loaded all 17 anthropics skills (incident 2026-08-03).
- Decision: The anthropics clone lives in `~/.pi/agent/vendor/anthropics`; only the four Wave 1 doc skills are registered via settings `skills`. External skill collections are referenced by path, never vendored into this repository, unless the license permits redistribution into a public repo and the skill is adapted.
- Alternatives: Vendor the four skills — rejected: redistribution of source-available material into a public repository.
- Rationale: License compliance and curated context (success criterion 4). Rules recorded in `capabilities/skills/NOTES.md`.

### D17 — Wave 2 layer decisions recorded early

- Date: 2026-08-03
- Context: The architecture requires the subagent-layer and browser-server conflicts to be decided before both sides coexist; Milestone 6 records the decisions, Wave 2 executes them.
- Decision: (a) Subagent layer — `pi-dynamic-workflows` wins the orchestration role; installed `pi-subagents` is retired when dynamic-workflows is integrated (Wave 2). (b) Browser server — playwright-mcp is the preferred E2E server per official token-economy guidance; the swap from chrome-devtools is evaluated during Wave 2 after a concrete E2E need is demonstrated. (c) Permission policy — full allow/ask/deny gates, bash policy, and external-directory guard are configured in Wave 2; Wave 1 keeps path protection only.
- Alternatives: Integrate both subagent layers — rejected: duplication cost exceeds benefit (benchmark).
- Rationale: Conflict resolution before coexistence; decisions recorded here, execution gated to Wave 2.

### D18 — Permission policy deepened (Wave 2)

- Date: 2026-08-03
- Context: D17 required full gates in Wave 2. Headless determinism matters (the validation battery runs `pi -p`).
- Decision: Policy = tools `allow` by default; path protection unchanged (secrets deny); bash policy — deny `rm -rf *`/`rm -fr *`, ask `git push --force*`/`sudo *`; external-directory — `ask` outside `cwd` with allows for platform dirs (`~/.pi/agent/**`, `~/.config/mcp/**`, `~/.claude/**`, `~/.codex/**`).
- Alternatives: external-directory `allow`-by-default — rejected: leaves a guard hole; `ask` is the designed CWD guard for interactive sessions.
- Validation: `.env` deny re-tested; `rm -rf *` deny confirmed (model fell back to plain `rm`); normal reads and bash unaffected. PASS.

### D19 — Superpowers methodology ported by reference; framework extension not adopted

- Date: 2026-08-03
- Context: Wave 2 methodology integration; obra/superpowers ships methodology skills plus a first-party Pi extension.
- Decision: Port the four highest-value methodology skills — test-driven-development, systematic-debugging, writing-plans, executing-plans — by reference from `~/.pi/agent/vendor/superpowers` (each verified framework-free: 0 `@`-commands). The superpowers Pi extension (hooks/commands flow) is NOT adopted.
- Alternatives: Adopt the full framework extension — rejected: its session-start hook flow couples to the whole system and adds surface without a proven need.
- Rationale: The methodology is the value; the framework is optional complexity. "Adapt before Adopt."

### D20 — Workflow consolidation and retirement executed

- Date: 2026-08-03
- Context: D17 chose pi-dynamic-workflows over pi-subagents; review duplication existed between the review template and the uninstalled Codex review suite.
- Decision: (a) pi-subagents retired — removed, registry row marked deprecated/retired. (b) dynamic-workflows adopted — launch validated (survey → fan-out → synthesis; completion async by design). (c) review template consolidated into the four-dimension Codex-derived methodology (correctness, security, breaking/scope, testing) deployed as `capabilities/prompts/review.md`.
- Alternatives: Keep pi-subagents installed alongside dynamic-workflows — rejected: duplication without benefit.
- Rationale: One workflow engine, one review methodology, minimal overlap — the Phase 3 consolidation principle.

### D21 — Pending validations completed

- Date: 2026-08-03
- Context: Wave-1 items remained pending: pdf functional conversion and license verification.
- Decision: pdf skill functionally validated end-to-end (real text extracted via pypdf; host deps `pypdf`, `reportlab` added); all 7 relevant package licenses verified MIT (registry updated). Interactive-dependent validations (plan-mode enforcement, ask-user prompting, pi-simplify, pi-lens) are load-validated and marked interactive-by-design.
- Alternatives: Defer — rejected: the evidence was obtainable.
- Rationale: Validation-before-promotion; honest statuses where interactive-only.

### D22 — Engineering Intelligence Layer v1 established

- Date: 2026-08-03
- Context: Milestone 8 mandate — build the first version of the intelligence layer without a separate engine (architecture design rule: intelligence = workflow composition).
- Decision: Implemented as platform-owned artifacts: `capabilities/skills/repository-intelligence/` (repo analysis protocol) and five prompt templates (`workflow`, `decide`, `plan-next`, `review-architecture`, `self-eval`), plus the orchestration rules, context strategy, and self-evaluation framework recorded in `docs/ARCHITECTURE.md`.
- Behaviorally validated 2026-08-03: repository-intelligence (full repo analysis produced), plan-next (correctly audited the uncommitted state and flagged missing registry rows + premature validation claim), decide (evidence-based browser-decision analysis). self-eval: load-validated; behavioral run interrupted by environment — pending (its dimensions were partially exercised by the plan-next audit).
- Alternatives: Build a dedicated decision engine service — rejected: violates the design rule; adds infrastructure without measured value.
- Rationale: The layer assists judgment via validated, composable protocols — no new architecture.

### D23 — Browser decision review (challenge confirmed)

- Date: 2026-08-03
- Context: The `/decide` protocol re-evaluated D17's playwright preference against current evidence.
- Decision: Keep chrome-devtools. D17's gate ("concrete E2E need") is not met — this repository contains no application under test; D13 had already rejected playwright for the same reason; token-economy claims are contested by conflicting benchmarks. chrome-devtools license verified Apache-2.0 (registry updated). Playwright swap remains gated on a demonstrated E2E need.
- Alternatives: Swap now, or run both — rejected: cost without need, and duplication violates the consolidation principle (D20).
- Rationale: Challenge assumptions with evidence; a prior decision (D17 preference) is refined without a new dependency, and no change is needed to DECISIONS/decision rules — the gate already encoded this outcome.


### D24 — Intelligence layer self-evaluation (autonomous DECISIONS write)

- Date: 2026-08-03
- Context: First `/self-eval` run. The working tree contained the intelligence-layer v1 artifacts (five prompts + `repository-intelligence` skill, deployed to `~/.pi/agent/prompts/` 8/8 in sync) and an `ARCHITECTURE.md` Phase 6 edit claiming "implemented Milestone 8", but the capture loop was open: no registry rows, no CHANGELOG entry, no decision entry, `PROJECT_STATE.md` still "In Flight: Nothing".
- Decision: (a) Adopt the intelligence-layer v1 artifacts as platform assets (registry rows + CHANGELOG entry follow in the same session per the capture loop). (b) Resolve the Milestone-8 numbering conflict (ARCHITECTURE.md calls this "M8" while ROADMAP defined M8 as Wave 3) in favor of ROADMAP's numbering; register or remove the unregistered `firecrawl` reference in orchestration rule 3.
- Alternatives: (a) Leave the artifacts deployed but undocumented — rejected: silent deployment violates the capture loop and the registry single-source rule. (b) Renumber Wave 3 — rejected: ROADMAP owns milestone numbering; the architecture section is the newcomer.
- Rationale: The repository's value is reflecting reality; the capture loop and registry are its enforcement mechanisms, and this evaluation surfaced their first live gap.
- Resolution (2026-08-03, M8 close): both recommendations executed — ROADMAP now numbers the intelligence layer as Milestone 8 and Wave 3 as Milestone 9; orchestration rule 3 no longer references the unregistered `firecrawl` (see `docs/ARCHITECTURE.md`). This entry was written by the self-evaluation run itself, demonstrating the layer's assisted-governance behavior; it was renumbered to D24 on reconciliation.

### D25 — Wave 3 integration decisions (curation over quantity)

- Date: 2026-08-03
- Context: Milestone 9 scope listed four advanced capabilities; each required justification and validation before integration.
- Decision: (a) **memory MCP integrated and validated** — keyless, reference-quality, pairs with the engineering memory strategy (D27). (b) **GitHub MCP deferred** — requires PAT provisioning (user action) plus a Windows prebuilt-binary install; no demonstrated PR/issue workflow need yet. (c) **piolium deferred** — sandbox host requirement unmet on this machine and 0.0.x maturity; re-evaluate when a containerized environment exists. (d) **pi-background-tasks deferred** — duplicates the dynamic-workflows orchestration/background role; the consolidation principle (D20) and one-engine-per-role rule reject it. (e) **playwright swap remains gated** (D23).
- Alternatives: Integrate all four — rejected: two lack credentials/host prerequisites, one duplicates an existing engine, none demonstrated need.
- Rationale: Quality over quantity; every deferred item has a recorded trigger for re-evaluation (registry Notes).

### D26 — Engineering quality metrics baseline

- Date: 2026-08-03
- Context: The `/metrics` protocol's behavioral run was degraded by the environment (output lost); the manifest itself is the deliverable.
- Decision: Compute the eight metrics directly from repository evidence (no model needed): maturity 79% (22/28 active), validation coverage 89% (25/28 PASS-validated), 24 decision entries, 13 open carried items, 0 broken links/orphans (scan), 4 deferred candidates, 1 UNVERIFIED license remaining (pi-plan-mode). Baseline recorded in the M9 report; `/metrics` remains available for future automated computation.
- Alternatives: Re-run until the environment cooperates — rejected: the numbers are deterministic arithmetic on repository state.
- Rationale: Evidence over ceremony; the metric values are what matters, not the channel.

### D27 — Engineering memory strategy adopted

- Date: 2026-08-03
- Context: Milestone 9 requires a production-ready engineering memory strategy; the memory MCP server is now integrated (D25).
- Decision: Layered strategy — repository (permanent, primary: DECISIONS/registry/SETUP/CHANGELOG via capture loop), project memory (`AGENTS.md` per working repo), session-scale scratch (memory MCP: transient entities/relations/observations; never personal, never credentials). Promotion rule: facts become repository entries the same session they become decisions or validated results; the memory store is never authoritative. Protocol: `/memory` template.
- Alternatives: Use the memory MCP as the primary store — rejected: violates Repository First; the repository is the source of truth.
- Rationale: Memory assists, never replaces, the repository (recorded in `docs/ARCHITECTURE.md`).

### D28 — Verification script adopted (lightweight, no CI)

- Date: 2026-08-03
- Context: Milestone 10 production hardening requires automated verification without CI complexity.
- Decision: Adopt `capabilities/scripts/verify.py` (Python stdlib only, no network, no dependencies) — checks markdown links, orphans, secret patterns in tracked files, structure conformance, registry shape (10 columns, unique names), DECISIONS numbering, TODO section hygiene. Run before every commit (CONTRIBUTING). First run caught the D24 ordering defect; all checks green after the fix.
- Alternatives: Full CI pipeline — rejected: violates the platform's own non-goals (no CI, D9/M5); this host has no runner.
- Rationale: A 150-line deterministic script replaces ad-hoc milestone scans; robustness over feature growth.

### D29 — Production hardening decisions

- Date: 2026-08-03
- Context: Hardening review of the only code file and the safety surfaces.
- Decision: (a) power-tools.ts type-check deferred — runtime smoke evidence exists (tools validated behaviorally 08-02); static type-checking requires module resolution against the global pi packages, which is fragile outside a repo-local dev setup; revisit with a dev setup (TODO). (b) Permission configuration remains host-side (`~/.pi/agent/extensions/.../config.json`), described not stored in the repository (secrets policy, D2/D3). (c) Memory MCP storage confirmed outside the repository (no `memory.json` in the tree). (d) Secret scanning now enforced by the verify script (D28).
- Alternatives: Add a dev toolchain to the repository for type-checking — rejected: adds dependency surface without a current consumer need.
- Rationale: Honest evidence-based hardening; the two real risks (secrets, consistency) get automated guards, the cosmetic one (type-check) gets a trigger.
- Resolution (2026-08-21): item (a) closed — strict type-check executed zero-footprint (TypeScript 7.0.2 via ephemeral `npx`; module resolution mapped to the global pi installation's bundled type declarations; no repo-local dev setup added). PASS, no findings; the fragility concern did not materialize.

### D30 — Foundation certification and transition to Continuous Evolution

- Date: 2026-08-03
- Context: Milestone 10 concludes the foundational phase; the platform must transition from construction to evolution.
- Decision: Certified per the M10 engineering certification (report): architecture stable, governance mature, maintainability acceptable, overengineering under control. From this milestone on: no predefined construction milestones; changes enter via the capability lifecycle, capture loop, and trigger-based integrations, driven by real engineering needs. Stable core: architecture, governance, lifecycle, registry, ownership matrix. Evolving: capabilities, intelligence versions, prompts, templates. Experimental features: isolated until validated (Wave-4 rule). Versioning: constitution v1.x via amendment; capability pins in the registry.
- Alternatives: Continue milestone-driven construction — rejected: the platform is built; further construction would be feature growth without need.
- Rationale: The certification is evidence-based; evolution-by-need is the long-term sustainability posture.

### D31 — Repository consumption scope resolved: single primary environment

- Date: 2026-08-21
- Context: Open question (carried since Milestone 2 in `PROJECT_STATE.md` and `NEXT_SESSION.md`): whether other machines or agents consume this repository as their source of truth. It could not be determined from repository evidence and required user input. Consumption scope calibrates governance: multiple consumers would require stability guarantees, deprecation notice practices, and change coordination.
- Decision: Resolved per user input (2026-08-21): the repository is the single source of truth for **one primary Harness Pi environment**; no other machines or AI agents actively consume it as their source of truth. Governance remains single-consumer: no stability/deprecation contract beyond the capture loop; ownership matrix, registry schema, and lifecycle unchanged. Re-open trigger: a second machine or agent begins consuming the repository as its source of truth — then revisit change-notice and cold-install cadence practices.
- Alternatives: (a) Assume multi-consumer and add stability machinery (versioned contracts, deprecation windows) — rejected: complexity without a demonstrated need (Never Overengineer); (b) leave the question unresolved — rejected: governance calibration was blocked on it.
- Rationale: Governance scope is now recorded from authoritative input rather than assumption; the re-open trigger keeps the decision honest if reality changes.

### D32 — MCP server version pinning executed (architecture compliance)

- Date: 2026-08-21
- Context: An audit of the live runtime and repository assets during the Continuous Evolution capability assessment identified that all four configured MCP servers (`chrome-devtools`, `sequential-thinking`, `context7`, `memory`) were configured with unpinned commands (`@latest` or unversioned npx commands). This contradicted the governance rule in `docs/ARCHITECTURE.md` Phase 4 ("capabilities pinned at integration") and introduced supply-chain and reproducibility risks.
- Decision: Pin all four MCP servers to concrete, verified versions in both the live configuration (`~/.config/mcp/mcp.json`) and repository fragments (`capabilities/mcp/*.md`): (a) `chrome-devtools-mcp@1.7.0` (Apache-2.0, 29 tools discovered), (b) `@modelcontextprotocol/server-sequential-thinking@2026.7.4` (MIT, tool call verified), (c) `@upstash/context7-mcp@4.0.3` (MIT, library search tool call verified), (d) `@modelcontextprotocol/server-memory@2026.7.4` (MIT, entity creation tool call verified). Created missing server note `capabilities/mcp/memory.md` and updated `docs/SETUP.md` and `capabilities/index.md`.
- Alternatives: (a) Retain `@latest` pulls — rejected: leaves runtime vulnerable to breaking changes and upstream drift; (b) pin to historical cache versions without verification — rejected: latest stable releases were verified healthy and functioning via direct JSON-RPC stdio testing.
- Rationale: Closes the gap between architecture mandate and live reality; guarantees reproducible MCP behavior across sessions while preserving the single-proxy adapter architecture.

### D33 — Runtime Abstraction Layer v1 adopted (runtime-orchestrator extension)

- Date: 2026-08-22
- Context: The runtime simplification audit (2026-08-21) identified that the platform's biggest operational friction was the manual sync between the Blueprint Control Plane and the Pi runtime, plus zero automatic project context at session start. The audit proposed a Runtime Abstraction Layer (RAL) implemented as a minimal Pi extension, not a new daemon or framework.
- Decision: Implement RAL Phase 1 as `capabilities/extensions/runtime-orchestrator.ts`: (a) `session_start` hook for project topology detection and 9router supervision; (b) `before_agent_start` hook for minimal workspace context injection (~30–50 tokens); (c) `/doctor` command for runtime health diagnostics. Zero external dependencies (Node.js stdlib only). TypeScript strict type-check PASS (TS 7.0.2). Deployed to `~/.pi/agent/extensions/runtime-orchestrator.ts`. Phase 2 (`/sync` command) and dynamic skill activation are explicitly deferred.
- Alternatives: (a) Extend `power-tools.ts` with orchestrator behaviour — rejected: power-tools owns `repo_tree`/`git_log` tools; mixing runtime lifecycle orchestration into it conflates two distinct ownership contracts; (b) build a separate background daemon — rejected: the architecture audit rule "no new daemons"; (c) implement the full RAL at once — rejected: Phase 1 only delivers the minimum needed to prove the lifecycle hook pattern before adding sync/skill-activation complexity.
- Rationale: The extension lifecycle APIs (`session_start`, `before_agent_start`, `registerCommand`) are exactly the hooks needed. A single ~300-line TypeScript file with zero dependencies closes the biggest daily-ergonomics gap without touching the governance layer.

### D34 — RAL v1 Phase 2 adopted: /sync one-way asset deployment engine

- Date: 2026-08-22
- Context: While RAL Phase 1 solved startup ergonomics and project awareness, changing an asset in the Blueprint repository still required manual file copying into `~/.pi/agent/` to take effect in the runtime.
- Decision: Implement RAL Phase 2 one-way asset synchronization directly inside `capabilities/extensions/runtime-orchestrator.ts` via the `/sync` command and `executeSync()` engine: (a) strictly one-way (Blueprint → `~/.pi/agent`); (b) explicit allowlist scope (`capabilities/prompts/*.md`, `capabilities/extensions/*.ts`, `capabilities/skills/repository-intelligence`); (c) SHA256 content hash drift detection comparing source, runtime, and previous baseline (`sync-state.json`); (d) safe conflict handling (local runtime changes are preserved and flagged as conflicts unless `--force` is provided); (e) protected file isolation (`auth.json`, `models.json`, `settings.json`, `mcp.json`, `sessions/` are never overwritten); (f) dry-run preview by default, execution via `/sync --apply`. Integrated sync status checks into `/doctor`.
- Alternatives: (a) Standalone Python script (`sync.py`) — rejected: having `/sync` inside the Pi extension enables in-session 1-command reconciliation without leaving the editor, while reusing the same TypeScript codebase; (b) bidirectional sync — rejected: violates the Repository First principle (the runtime must never become an unvetted source of truth); (c) blind copying without drift detection — rejected: would silently destroy local experimental edits without warning.
- Rationale: Eliminates the last manual copy-paste bottleneck between Control Plane and Runtime Plane while strictly enforcing safety, inspectability, and Blueprint single-source governance.

### D35 — RAL v1 Phase 3 adopted: per-turn in-memory capability scoping (Option E+)

- Date: 2026-08-22
- Context: Phase 3 required dynamic project-aware capability scoping. The original design proposal recommended Option A′ — toggling `disable-model-invocation` frontmatter in deployed SKILL.md files. The architecture challenge review rejected A′: it would mutate deployed Blueprint-owned assets (breaking the D34 invariant "runtime copy = deployed replica"), create `/sync` drift-detection coupling requiring a marker whitelist, introduce multi-file atomicity/recovery risks, and leave a first-turn staleness question unresolved.
- Decision: Implement **Option E+** — pure in-memory, per-turn skill-index scoping inside `runtime-orchestrator.ts`: (a) `capabilities/scopes.json` as a Blueprint-owned tag map (`core` floor + `scopes` name→tags); (b) `mapTopologyToProfile()` deterministic topology→profile mapping reusing the existing detector; (c) `resolveCapabilitySets()` computing ACTIVE = CORE ∪ {tag∩profile ≠ ∅}, AVAILABLE = rest; (d) `renderFilteredSystemPrompt()` replacing only the `<available_skills>` section of the system prompt via the supported `before_agent_start` chaining API; (e) fail-open on any failure (all skills stay visible); (f) native `/skill:<name>` as the escape hatch (verified to expand hidden skills); (g) `/doctor` extended with Profile/Active/Available/Evidence/Governance sections; (h) scopes.json added to the `/sync` allowlist. No SKILL.md mutation, no `/sync` semantic changes, no LLM classifier, no daemon/database.
- Alternatives: (a) Option A′ file markers — rejected per architecture challenge review (ownership violation, sync coupling, atomicity risk, timing gap); (b) Option C project-local `.pi/settings.json` skills[] override — rejected: grants projects authority over the capability set, inverting Blueprint governance; (c) task-aware activation — deferred without usage evidence.
- Rationale: Pi 0.83.0 exposes the loaded `Skill[]` and a chained per-turn systemPrompt replacement through documented extension APIs; filtering in memory achieves true first-turn-correct default visibility with zero durable-state changes. Fail-open guarantees scoping failure can never reduce capability availability below today's baseline.

### D36 — RAL Phase 4 adopted: Pi-native dynamic model catalog bridge (Option C)

- Date: 2026-08-22
- Context: Real-world validation showed `/doctor` reporting "203 models available" from the live 9router while `/model` exposed only the ~85 statically frozen entries in `models.json`. Root cause: the statically configured 9router provider defines no model-refresh mechanism, so its catalog can never track the router. The architecture audit verified that Pi 0.83.0 natively supports dynamic provider catalogs via `ProviderConfig.refreshModels(context)` — invoked automatically on `/model` open and candidate listing for every provider that defines it.
- Decision: Implement **Option C** inside `runtime-orchestrator.ts`: register a dynamic overlay for provider id `"9router"` via `pi.registerProvider("9router", { name, baseUrl, api: "openai-completions", models: [], refreshModels })`. `refreshModels` fetches `/v1/models` through the shared RAL transport and maps entries deterministically (`mapRouterModelToPi`): id/name from router id; `reasoning` from capabilities; `input = ["text"] (+["image"] if vision)`; `contextWindow = context_length`; `maxTokens = max_completion_tokens`; zero cost (no pricing source); canonical thinkingFormats passthrough only (`openai/deepseek/zai` observed) with non-canonical formats omitted rather than fabricated. Fail-open: malformed entries skipped, deduplicated; empty mapped result falls back to prior stored catalog; offline context (`allowNetwork=false`) returns store-only; all refresh errors are isolated by Pi's per-provider handling and never blank the catalog or crash startup. Only provider id `"9router"` is registered — unrelated providers untouched. User-defined custom models convention: use a separate provider id in `models.json`; RAL never touches it.
- Alternatives: (a) Reconcile `models.json` from Blueprint/discovery — rejected: violates the protected-file ownership invariant (D34-class), creates a second writer on runtime/user state; (b) in-memory injection without `registerProvider` — rejected: no supported hook, would require unsupported internals; (c) hybrid static+dynamic — rejected: router metadata is complete (203/203 field coverage), availability already native.
- Rationale: Pi ships the exact abstraction for this problem; bridging discovery through the documented extension surface achieves first-session-correct catalog freshness with zero durable-state changes, no daemons, no polling, and strict ownership boundaries.

### D37 — Complexity-aware orchestration & independent reasoning profiles adopted

- Date: 2026-08-23
- Context: Real-world cafe-mockup validation showed standing `/effort ultra` auto-arming a workflow on a simple task; the ULTRA directive ("fan out widely… set a high maxAgents") led the model to script 9 agents and request `maxAgents: 60`, hitting `Agent limit exceeded (60)` (session `01a02fb7`). Root cause: effort level was the only input to execution scale — no complexity judgment existed anywhere in the workflow package. A separate gap: workflow model selection had no current-model default, and reasoning configuration was conflated with model tiers.
- Decision: Implement complexity-aware orchestration + independent reasoning profiles in `runtime-orchestrator.ts` (D37). (a) An orchestration governance contract is appended to every system prompt: effort = reasoning depth, never agent count; scripts MUST declare `// complexity: DIRECT|LIGHT|FULL|HEAVY` plus workstreams/parallelizable/risk. (b) The extension intercepts the `workflow` tool call and enforces strategy caps: DIRECT=1, LIGHT≤3, FULL≤8; HEAVY requires explicit user approval via an interactive select (Multi-agent / Single-agent / Let Harness decide), with the explanation shown before the choices. Requested maxAgents are clamped into the approved ceiling — the model cannot bypass via 60/100/200. (c) Model policy defaults to USE CURRENT MODEL: sub-agents inherit the session model; a tool-call guard detects explicit model overrides in scripts and prompts the user (Keep current / Allow) rather than switching silently. (d) `/reasoning <Default|Plan|Review> <level>` stores user-controlled reasoning profiles (runtime-owned JSON, independent of model tiers); the active profile/level is injected into the system prompt so workflow scripts apply matching thinking suffixes.
- Alternatives: (a) Raising maxAgents — rejected: the limit was never the problem, judgment was; (b) modifying pi-dynamic-workflows internals — rejected: third-party package, not Blueprint-owned; enforcement at the tool boundary via the supported `tool_call` event is equivalent and upgrade-safe; (c) AUTO model selection from the live catalog — deferred: router availability ≠ end-to-end usability, user knowledge wins; (d) LLM-based complexity classifier — rejected: the contract makes the already-engaged model assess complexity inline with structured tags, deterministic and explainable.

### D38 — Integrated /model reasoning flow adopted (model + profile + level in one flow)

- Date: 2026-08-23
- Context: Phase 5 shipped reasoning profiles via a command-based UX (`/reasoning <profile> [level]`). Real-world use showed the preferred interaction is a single coherent flow: `/model` → select model → select reasoning profile → select reasoning level → confirm. Pi's native `/model` selector already auto-refreshes providers on open (D36 bridge), so no new discovery mechanism was needed.
- Decision: Implement the integrated flow via Pi's native `model_select` extension event: after the user selects a model in `/model`, RAL presents a Reasoning Profile selector (10 profiles; Vision offered only when `model.input` includes image) followed by a Reasoning Level selector (user-facing Off/Low/Medium/High/Ultra mapping to canonical runtime values off/low/medium/high/xhigh — "Ultra" is an alias for xhigh, not a new runtime value). The chosen profile+level persist in `harness-reasoning.json` (runtime-owned) and are applied via `pi.setThinkingLevel()` (native setter clamps to model capabilities). Selection is optional — cancelling keeps the previous configuration. `/reasoning` remains as a CLI-compatibility path. Model/profile/tier separation preserved: profile selection never changes the session model or tier assignments, and the D37 silent-switch guard is unchanged.
- Alternatives: (a) Injecting steps into Pi's native /model selector component — rejected: host-owned TUI component, not extension-modifiable without unsupported internals; (b) separate `/model-reasoning` command — rejected: fragments the flow the task explicitly asked to unify; (c) automatic profile inference from task type — deferred per D37.
- Rationale: Uses only documented extension surfaces (`model_select` event, `ctx.ui.select`, `pi.setThinkingLevel`) to deliver the one-flow UX; capability gating comes free via the native clamp plus explicit Vision filtering.

### D39 — Model Control Center (`/mcc`) adopted: multi-profile reasoning UX

- Date: 2026-08-23
- Context: The D38 integrated `/model` flow configures one profile per invocation. Real-world use showed users need independent persistent configuration for multiple profiles (e.g., Plan=High, Task=Medium, Commit=Low) in one session, re-enterable at any time without `/reasoning`, with current levels visible at a glance.
- Decision: Implement the Model Control Center as the `/mcc` command in `runtime-orchestrator.ts`: a grouped overview screen (GENERAL / PLANNING / EXECUTION / SPECIALIZED) rendering all 10 profiles with their effective level at a glance plus the current model; selecting a profile opens a focused Reasoning Level editor (Off/Low/Medium/High/Ultra) that persists immediately per edit (cancel-safe); "Select Model" switches models via `ctx.modelRegistry` + `pi.setModel`; Vision is gated on image-input capability. State model upgraded to v2 (`harness-reasoning.json`: activeProfile/activeLevel/overrides), migrating legacy single-pair files. `/reasoning` and the D38 `/model` post-select flow remain for compatibility.
- Alternatives: (a) Extending the host-owned `/model` TUI component — rejected: not extension-modifiable without unsupported internals; (b) one-shot wizard only — rejected: fails multi-profile requirement; (c) new UI framework — rejected: uses Pi-native pi-tui primitives (`ctx.ui.custom`, Container/Text/Spacer/SelectList) exactly as `/workflows-models` does.
- Rationale: Delivers the polished, re-enterable Model Control Center UX while keeping every D36/D37/D38 invariant — no models.json/auth.json writes, no second discovery mechanism, no silent model switching.
### D40 — MCC UX refinement: viewport-limited model picker, unknown-model fix, immediate overview updates

- Date: 2026-08-23
- Context: Real-world testing of D39's `/mcc` exposed three concrete issues: (1) the model picker rendered all ~203 models at once via `ctx.ui.select` (whose `ExtensionSelectorComponent` has no viewport limiting), flooding the terminal; (2) the overview showed `unknown/unknown` as the current model when `ctx.model` held Pi's placeholder DEFAULT_MODEL (provider/id/api all `"unknown"` before a real selection); (3) profile level changes were not immediately reflected in the overview.
- Decision: Refine `/mcc` in place (D39.1, no architecture change): (a) model picker rebuilt on pi-tui `SelectList` via `ctx.ui.custom` with `maxVisible=12` — viewport-limited rendering with native scroll, `→` selection prefix, theme-driven highlight, and type-to-filter using pi-tui's own prefix filter (`setFilter`); (b) placeholder detection treats provider+id both `"unknown"` as "no current model", displaying `(none — choose below)` instead of `unknown/unknown`; (c) profile edits persist immediately and the overview loop re-renders from fresh state each iteration, so saved values appear without reopening; (d) profile editor uses radio-style ●/○ indicators with Enter=save / Esc=cancel semantics. All D36/D37/D38 behavior unchanged.
- Alternatives: (a) Raising maxAgents or paging through ctx.ui.select — rejected: root cause was missing viewport handling, not list size; (b) building custom scrolling — rejected: SelectList already implements viewport-limited rendering natively via its maxVisible constructor parameter; (c) hardcoding "unknown" label suppression — rejected: fixed the underlying state/display mismatch per task requirements.
- Rationale: Uses only existing Pi TUI primitives (SelectList already viewport-limits internally); no new UI framework or discovery mechanism. The unknown/unknown fix addresses the state/display mismatch rather than masking it.


### D41 — MCC real-world UX defect fixes: profile-key parse bug, grouped structural overview, fuzzy router-first model picker, unified v2 persistence

- Date: 2026-08-25
- Context: Real-world TUI validation of D40's `/mcc` broke three of its verified claims. (1) Overview rows encoded values as `__p_<Name>__` while the editor parsed with `slice(4)`, yielding `Task__`-style keys: every save wrote overrides under corrupted keys (a live `"Plan__"` was found in `harness-reasoning.json`), the editor showed header `TASK__` / `Current level: undefined`, and load-time sanitization silently discarded the writes — so "immediate overview updates" never actually displayed a change. (2) D39.1 had fixed selectable headers by deleting them entirely, losing the GENERAL/PLANNING/EXECUTION/SPECIALIZED information architecture; the active marker also shared the selection highlight's styling. (3) The model picker's prefix-only filter returned "No matching commands" for mid-string queries ("stealth" against `9router/openrouter/stealth/ox-alpha`) on a catalog that had grown to 1356 models, and opened at `amazon-bedrock/*`. Additionally, `model_select` and `/reasoning` still wrote legacy v1 state via `saveReasoningProfile`, clobbering v2 `overrides` on every use, and `loadReasoningProfile()` read only legacy keys so it could not see v2-era active profiles.
- Decision: Fix in place (no architecture change). (a) Value tokens are produced by one exported helper (`mccProfileValue`) and resolved through a value→profile Record built in the same loop that builds rows — no string surgery. (b) Purpose-built `MccOverviewList` component on pi-tui primitives (`Component` + theme fns + `getKeybindings`): group headers and capability-disabled rows (Vision without image input) render structurally but are skipped by navigation; arrow keys wrap across selectable items only; columnar primary/description layout; success-colored `●active` marker distinct from the `selectedBg` highlight; viewport capped at maxLines with an `(n/N)` indicator, leading-spacer and trailing-orphan-header trim. (c) Model picker uses pi-tui `fuzzyFilter` (subsequence, token-aware), sorts 9router models first, shows match counts, supports Delete-key editing. (d) Persistence unified on v2: `loadReasoningProfile()` delegates to `loadReasoningStateV2()`; `model_select` and `/reasoning` update `activeProfile`/`activeLevel` plus the profile override; `saveReasoningStateV2()` prunes invalid keys/levels on write (heals corrupt files); `saveReasoningProfile()` removed. (e) Level editor preselects the current radio and offers explicit "Keep current (<raw>)" when the stored level is outside USER_LEVEL_MAP (removes the silent Off trap). (f) `session_start` warms `ctx.modelRegistry.refresh()` after router health OK.
- Alternatives: (a) Fixing only the slice offset — rejected: left the flat list, prefix-only filter, and v1/v2 clobbering untouched; (b) SelectList with injected pseudo-header rows skipped by index math — rejected: SelectList styles every row uniformly, so headers could never be visually structural; a purpose-built component over public pi-tui primitives is smaller than the workarounds; (c) Migrating the state file to a new schema version — rejected: sanitization-on-write heals existing files with zero migration risk; (d) Auto-applying `settings.json` defaultModel at boot when the session model is unresolved — deferred: touches the silent-model-switch invariant and depends on Pi-core provider-refresh timing; recorded as an open observation instead.
- Rationale: Evidence-first: every fix traces to an observed defect in a live session. Validation: strict type-check PASS (TS latest); 12-check headless harness PASS (headers/disabled unselectable across widths 58–140; full cycle visits exactly 11 selectable items with wrap both ways; marker spacing + distinct color span; columnar gaps ≥2 cols; width-36 drops descriptions cleanly; viewport ≤15 lines with indicator; Enter emits exact encoded values; router-first sort); live TUI: Commit low→High saved with notify `Commit → high`, overview row updated in place, state file healed to `{Default: high, Commit: high}` with no corrupt keys, reopen preserved configuration, picker opened on `9router/*` and `stealth ox` filtered to exactly ox-alpha (1/1356).

### D42 — Model Control System Phase 1: visibility trust state, boot-default restoration, reasoning v3 (Option C), `/model` control-center flow

- Date: 2026-08-25
- Context: The D42 architecture proposal and pre-implementation audit locked the model: native `/model` is already provider-scoped (availability snapshot, 203 = router catalog, zero phantoms; the 1356 phenomenon came from getAll()-based custom surfaces); `openrouter/stealth/ox-alpha` is present/selectable end-to-end and the real defect was that declared defaults never auto-apply at boot (static-list resolution precedes dynamic refresh); 9router admin truth (CONNECTED/ENABLED) is session-gated with no reusable token in 0.5.55; runtime tier resolution never silently invents models but inline `{ model: "..." }` options-object overrides bypassed the D37 line-start guard.
- Decision: Implement Phase 1. (a) Visibility trust state `~/.pi/agent/harness-models.json` ({visible:null|ids, hidden[], names{}}) applied inside refreshModels() — DISCOVERED ∩ VISIBLE = SELECTABLE; connectivity honestly labeled UNVERIFIED (admin-derived CONNECTED/ENABLED deferred to Phase 2 behind an explicit user consent file). (b) Boot-default restoration: bounded startup handshake (8 × ~900 ms, stops on success) restores the DECLARED settings default only when ctx.model is the placeholder, the default exists in availability, and it is visible; emits a compact notice; suppresses the model_select dialog for restoration-originated selections. (c) Reasoning state v3 {version:3, defaultProfile, profiles} with sanitized single writer, v2/v1 migration preserving user values, and one pure resolver resolveEffective() (execution context wins over default; never mutates configuration). (d) Execution Profile: ephemeral, set at the workflow tool boundary from a validated `// profile:` header tag, window-scoped in memory only; sub-agents receive resolved values via injected prompts; scripts attempting to write harness-reasoning.json / harness-models.json are blocked or require explicit run approval. (e) `/model` post-selection control center (the MCC UI relocated onto the sanctioned flow): grouped structural headers, columnar rows, ★default marker distinct from selection highlight, immediate overview refresh from the single store, Set-as-Default action, SELECTABLE-only fuzzy picker with display-name enrichment (public /api/v1/models/info, capped/cached/best-effort). (f) `/mcc` command removed entirely. (g) Guard hardened: inline options-object model overrides are now detected and stripped on Keep-current. (h) `/doctor`: catalog counts, UNVERIFIED connectivity line, reasoning summary, placeholder blind spot fixed.
- Alternatives: (a) Replacing builtin `/model` via extension command — impossible (conflicting names are demoted to an alternate invocation); enriching the native surface + post-selection flow is the compliant realization. (b) Admin-session cookie reuse now — rejected for Phase 1: secret-at-rest + expiry fragility vs locked no-unnecessary-secrets rule. (c) Keeping v2 state — rejected: conflates active/default and duplicates level state; v3 removes the stale-preview class structurally.
- Rationale: Every mechanism rides documented Pi surfaces (registerProvider replacement semantics, availability snapshots, ui.custom, model_select, tool_call boundary). Validation: strict type-check PASS (TS latest); 15-check regression suite PASS (migration matrix incl. exact-value preservation, sanitizer, resolver purity + execution override without mutation, profile-tag parsing, protected-state guard, visibility filtering incl. stale ids, router-first sort, structural-header navigation across widths, stale-preview High→Low/Medium round-trip, ★default marker color span, viewport cap); live TUI battery PASS (clean-boot restore notice with footer ox-alpha @200k; control-center IA with UNVERIFIED chip + groups + (1/N) viewport; Vision high→low reflected immediately with Plan untouched; Set-Default persisted with marker move; DIRECT workflow with // profile: Review completed OK leaving configuration byte-identical; bypass attempt triggered Model-change-requested prompt and Keep-current stripped inline overrides).

### D43 — `/model` same-model dead-end: root cause (host skip), Alt+M / bare-`/reasoning` control-center access, error surfacing

- Date: 2026-08-25
- Context: Real-world testing showed `/model` → selecting the already-current model ended with no reasoning flow. Root cause found in Pi host source (agent-session.js `_emitModelSelect`, identical in 0.83.0 and 0.84.3): the event is skipped entirely when `modelsAreEqual(previous, next)`; additionally interactive `/model` is textually hard-coded in interactive-mode.js `setupEditorSubmitHandler` before extension dispatch, so neither extension commands nor input events can intercept it. Separately discovered: `/effort` is pi-dynamic-workflows' standing workflow auto-arm toggle (off/high/ultra) — it never touches harness reasoning state, explaining why "reasoning configuration via /effort" felt disconnected.
- Decision: Within host constraints (no fork, no hacks): (a) changed-model selections continue to open the control center via model_select (verified live); (b) reasoning-only access is provided by **Alt+M** (registerShortcut) and by **bare `/reasoning`**, both opening the SAME runModelControlCenter flow and the SAME v3 state — no duplicate state, no new command names; (c) model_select / control-center errors now surface via visible notify instead of silent catch; (d) `/doctor` gained a discoverability line; (e) upstream recommendation recorded: request Pi to always emit model_select (or add an extension override flag) so same-model confirmations can trigger post-selection flows.
- Alternatives: (a) Upgrading Pi — rejected as fix: 0.84.3 inspected; identical skip. (b) Registering an extension command named "model" — rejected: interactive submit handles "/model" textually before extension dispatch (demoted to invocationName only). (c) InputEvent transform of "/model" — rejected: TUI slash input never reaches the session input pipeline. (d) fs-watching settings/session for same-model writes — rejected: polling/watcher violates locked non-goals.
- Rationale: Honest engineering — the host limitation is documented with source references instead of a fragile bypass; all reasoning configuration remains reachable in two keystrokes through sanctioned surfaces, sharing one v3 store and resolver with the changed-model path.

### D44 — Unified `/model` realized via version-guarded host bridge (same-model selections now open the control center)

- Date: 2026-08-25
- Context: D43 proved Pi never emits `model_select` when the selected model equals the current one and that interactive `/model` is hard-coded before extension dispatch — leaving same-model confirmations (the natural "open model settings" gesture) as a dead end. The user locked the requirement that `/model` alone must realize the full model→profile→level flow and authorized a minimal, isolated host compatibility patch where no supported hook exists.
- Decision: Ship a surgical, version-guarded bridge. (a) `capabilities/scripts/pi-model-bridge.mjs` (status/apply/restore) locates the active pi-coding-agent install, refuses versions outside 0.83.x–0.84.x or with a changed `_emitModelSelect` structure, keeps a pristine backup (`agent-session.js.pi-bridge-orig`), is idempotent, and transforms ONLY the equality early-return into an emission of the SAME event with `sameModel: true` — no other behavior touched; `restore` returns the pristine bytes. (b) The extension gates the control center on `shouldOpenControlCenter()` ({sameModel:true, source:"set"} = user re-confirmed via selector → open; other programmatic emissions ignored) and surfaces handler errors visibly. (c) `/doctor` reports bridge state per installed version.
- Alternatives: staying with Alt+M // `/reasoning` as primary — rejected by requirement; upgrading Pi — rejected: 0.84.3 inspected, identical skip; InputEvent/command interception — impossible (host dispatch order); fs-watchers — polling non-goal.
- Rationale: The bridge is the smallest possible delta at the exact missing lifecycle point, fully reversible, self-diagnosing in /doctor, and honest in documentation: it is a compatibility layer, not an official Pi extension surface. Validation: applied to 0.83.0 (backup sha 9720d2a16054); live battery — same-model Enter opened the control center (the critical regression case), different-model selection auto-opened it with header/footer/model persistence consistent, Vision gating tracked the new model's capabilities, doctor reports APPLIED with counts/tip; strict type-check PASS; 16-check regression suite PASS (incl. shouldOpenControlCenter decision matrix + modelBridgeStatus); verify.py OK.

### D45 — `/model` width-overflow crash fixed with responsive rendering

- Date: 2026-08-25
- Context: Real-world use crashed Pi with `Rendered line 40 exceeds terminal width (215 > 204)`. Arithmetic reproduced exactly: `MccOverviewList.renderItem` padded `primary` to `labelCol` and then appended the marker padded to `labelCol − primaryWidth`, so the effective label column grew to `labelCol + (labelCol − primary)` while the description budget still assumed `labelCol`. Live values: prefix 2 + 25 + 11 (marker pad for `★default`) + 177 (description) = 215 > 204. Chrome lines (`Current model: …`, `Connectivity: UNVERIFIED — router discovery…`, footer hints, picker overlay) were also unbounded.
- Decision: Fix at the component rendering level. (a) `labelColumnWidth()` now derives the column from content, terminal width and a reserved description budget; (b) primary and marker share ONE label column (marker can never widen the row) with display-width-aware truncation and ellipsis; (c) every line passes through `fitLine()`/`clampLines()` final guards measured with pi-tui `visibleWidth` (ANSI and CJK/emoji aware, never `.length`); (d) responsive chrome via a `WidthSafeText` component — narrow terminals simplify wording (`Model:`/`Conn:`-style collapse, short footer) instead of shrinking abbreviations arbitrarily; (e) `/doctor` reports `/model UI: ✓ responsive width-safe`.
- Alternatives: hardcoding a max width or trusting a wide terminal — rejected: crash is width-dependent; truncating every string by length — rejected: would corrupt ANSI-styled lines; introducing a layout framework — rejected against locked simplicity rules.
- Rationale: The invariant is `renderedWidth <= availableWidth` for every line at every width, enforced by construction plus a final clamp, and verified to fail loudly when the old math is reintroduced (test non-vacuity proven). Validation: 18-check suite PASS including two overflow regressions across widths 8–400 (covering 204/215) with long IDs, long descriptions, Unicode markers and the selected-row background branch; strict type-check PASS; live: `/model` at 204 columns opens the control center with no crash, navigation across marked/unmarked rows stable, picker filter with a 60+ character ID safe; verify.py OK.

### D46 — `/model` Model Control Center visual refinement (table layout, hierarchy, responsive chrome)

- Date: 2026-08-25
- Context: D45 left the `/model` control center functional but flat and generic (user feedback): flat single-column rows, marker colliding with the next column, unbounded chrome text, a full-width selection bar, and dead space on wide terminals.
- Decision: Visual-only refinement, no architecture changes. (a) Rows render as a deliberate table — `name | level | description` — with dynamic column sizing derived from content, the level column, and the terminal width; the `★default`/`●active` marker lives INSIDE the name column and can never widen the row (D45 invariant preserved). (b) Level emphasis via semantic theme tones (dim→muted→text→accent for off/low/medium/high/ultra) — no hardcoded colors. (c) `padCell()` guarantees at least one trailing space so adjacent columns can never abut; every rendered line passes `fitLine()`/`clampLines()` final width clamps. (d) Header rebuilt as MODEL / REASONING blocks: the active model is bold and primary, provider + `Connectivity unverified` is muted metadata, default/execution profile shown with `★`/`●` markers. (e) Chrome (status, footers, picker, editor) uses `WidthSafeText` — content is built from the ACTUAL render width, so narrow terminals simplify wording (`Model:`, `Conn:`, `↑↓ · Enter · Esc`) instead of overflowing; `shortModel()` keeps the useful tail of long model ids. (f) Selection is now a subtle, bounded background highlight (no full-width bar); section rules are restrained (capped length), not full-bleed.
- Alternatives: full-width separators / checkbox grids / hardcoded colors — rejected as visual noise; fixed-width columns — rejected (D45 regression risk); adding a layout framework — rejected against locked simplicity rules.
- Rationale: information clarity + intentional hierarchy + restrained polish, all on Pi-native primitives and theme tokens. Validation: 21-check regression suite PASS incl. two overflow suites over widths 8–400 and a new LAYOUT regression proving name/level columns never abut (verified non-vacuous by reintroducing the defect); strict type-check PASS; live: `/model` at width 120 renders the table with `★default` separated, no crash; the D46 debug/SEP markers confirmed the live renderer matches the source (earlier "Taskoff" readings were a log-compositing artifact, not the UI). verify.py OK.

### D47 — `/model` navigation + detail visual architecture (two-pane control surface)

- Date: 2026-08-25
- Context: D46 left `/model` as a single full-width list — functional and width-safe but visually flat and generic. The user approved a larger visual redesign toward a navigation + detail control surface (OMP-inspired hierarchy, not a clone).
- Decision: Introduce a `NavDetailPane` composite over the existing `MccOverviewList` — above width 100 the navigation list renders in a left column (~40%) with a focused detail panel on the right; below it stacks (navigation, then detail). The detail panel explains the focused row: `CURRENT MODEL` (label, provider, context window, reasoning/vision capabilities) for the model context; `PROFILE` detail (name, purpose, effective level with semantic tone, `● Active` / `★ Default` / `● Execution` state, "Enter — edit reasoning" affordance) for profile rows; `Done — save & exit` for the action row. The model picker gained a `SELECTED MODEL` detail strip (label, provider, ctx, capabilities) that updates on highlight via `SelectList.onSelectionChange`, plus current-model indication. The list exposes `onSelectionChange`/`getSelectedItem()`; all new lines are width-clamped (D45 invariant).
- Alternatives: hand-built two-pane layout engine — rejected (locked simplicity rule; `NavDetailPane` is ~60 lines over existing primitives); true independent panes with separate scroll — rejected (no host layout engine; stacked fallback is the honest narrow-terminal mode); keeping the single column — rejected (user explicitly approved the redesign).
- Rationale: information hierarchy now matches the user's mental model (navigation chooses context, detail explains and edits it) while reusing every existing correctness guarantee. Validation: strict type-check PASS; 21-check regression suite PASS incl. two new NAV+DETAIL tests (two-pane at wide / stacked at narrow / never overflow across 40–300; focused detail updates on arrow navigation) and the D45 overflow suites (8–400); live at width 120: control center renders nav column + CURRENT MODEL detail, picker renders SELECTED MODEL + capabilities; verify.py OK.

### D48 — `/model` scope/navigation information architecture: provider navigation, scoped model browser, truthful counts

- Date: 2026-08-25
- Context: D47 established navigation + detail but the model list was still an unstructured global dump with `[9router]` tags on every row. The user identified that `OMP`'s information architecture is substantially clearer: provider navigation, scope visibility, selected-model metadata organized rather than dumped. The requirement was scope/navigation redesign without changing runtime behavior.
- Decision: Add a PROVIDERS navigation section to the overview with real selectable-model counts derived from the availability snapshot (`providerCounts`). The model picker is rebuilt as a two-pane `openModelBrowser` (provider scope | scoped model list) with a focus state (`providers` / `models`), scope-aware title (`9ROUTER MODELS` / `ALL MODELS`), truthfully counted providers, `✓` current-model marker, selected-model detail strip, and type-to-search within the scoped catalog. Both panes are side-by-side at width ≥ 100 (stacked narrow). The `—` level dash is suppressed for non-profile rows (providers, Done, model). Overview rows for providers dispatch to the scoped browser with the provider pre-selected. All existing D42–D47 behavior, D45 width safety, and D44 same-model bridge are preserved.
- Alternatives: keeping the single flat list — rejected (user explicitly requested scope/navigation); adding a separate provider list before the models — rejected (two-pane is the cleaner Pi-native interpretation of OMP's provider navigation); fabricating provider markers — rejected; `●` markers avoided because connectivity is `UNVERIFIED` in Phase 1.
- Rationale: Every pane is a Pi-native SelectList over the existing availability snapshot; focus is toggled with ←/→ keys, which is consistent with the OMP mental model. Validation: strict type-check PASS; 24-check regression suite PASS incl. three new D48 SCOPE tests (provider counts truthful and sorted, scope title, counts derive from selectable specs only); live on 264-model catalog: overview PROVIDERS section shows `9router` with correct count, provider detail panel shows `264 selectable models`, scoped browser opens with `9ROUTER MODELS` title, `✓` current model marker visible, `stealth` search finds the current model; verify.py OK.

### D49 — `/model` panelization: semantic regions for current context, navigation+detail, and footer

- Date: 2026-08-25
- Context: Real-world feedback: the D48 `/model` surface was functionally correct but still read as "unrelated information dumped into one vertical stream" — model, reasoning, profiles, providers and navigation blended together with no strong semantic boundaries.
- Decision: Introduce a small number of meaningful visual regions (not per-row boxes). (a) `panelLines(title, lines, width, theme)` — a shared titled-rule panel primitive; every region is built from it, so the vocabulary is consistent and every line stays width-clamped (D45). (b) The overview renders four regions: title, `CURRENT MODEL` panel (model, provider, `Connectivity: Unverified`, default/execution state), `NAVIGATION · DETAIL` panel (the two-pane nav|detail composite), and a footer rule + hint. (c) `NavDetailPane` gains a subtle `│` vertical divider between navigation and detail in two-pane mode (stacked mode stays clean). (d) The model browser wraps its head in a `MODEL BROWSER` panel, the highlighted model detail in a `SELECTED MODEL` panel, and its footer in a rule + hint. No runtime semantics changed.
- Alternatives: heavy per-row ASCII boxes — rejected (would look retro/decorative and reintroduce width risk); background tint panels — rejected (pi-tui has no reliable per-panel background primitive without custom rendering); merging everything into one flat panel — that is the status quo the user rejected.
- Rationale: semantic containers over decoration; four regions answer the four questions (what is current, where do I navigate, what is the focused detail, what can I do). Validation: strict type-check PASS; 27-check regression suite PASS incl. three new PANELS tests (divider separates nav/detail in two-pane, absent in stacked; `panelLines` wraps content with a titled rule; panel lines clamp to width); live at width ~120: overview renders `CURRENT MODEL`, `NAVIGATION · DETAIL` with `│`, footer rule; browser renders `MODEL BROWSER` / `9ROUTER MODELS` / `SELECTED MODEL` panels; verify.py OK.

### D50 — `/model` unified Model Control Surface: three-region focus-driven layout

- Date: 2026-08-25
- Context: D49 established panelized regions but the model list was still a separate browser (`openModelBrowser`) and reasoning configuration required navigating away from the main surface. The user approved the D50 single-screen architecture plan (G:/blueprint-report/model-control-center-single-screen-plan-2026-08-25.md) and locked two corrections: (1) provider state must never claim "Connected" — use "Configured / available to Pi" + "Connectivity: Unverified"; (2) the detail panel must follow the currently focused pane (NAV vs MODELS) rather than hard-coded model precedence.
- Decision: Implement the unified `ModelControlSurface` component — a three-region responsive layout (NAVIGATION | MODELS | DETAIL at wide ≥ 140, NAV | MODELS+DETAIL at medium 100–139, stacked below 100). The surface replaces the old two-pane `NavDetailPane` and the standalone `openModelBrowser`; the model list lives permanently on the screen. Detail derives from the focused pane: NAV focus shows profile/provider detail; MODELS focus shows selected model metadata. The surface holds focus scope and search state, persisting it across loop iterations via a caller-owned state object. Provider rows distinguish configured (selectable, truthful counts) from registered-but-unconfigured (`○` dimmed disabled rows); the detail panel for providers states "Availability: Configured / available to Pi" and "Connectivity: Unverified" — never "Connected". The `providerDetailLines` function was updated accordingly. The `persistInto` mechanism keeps the user's focus, provider scope, and search filter alive across model selections and profile edits.
- Alternatives: keeping the two-pane `NavDetailPane` + standalone browser — rejected (user explicitly wants a single surface); fabricating `● Connected` markers — rejected (correction #1); hard-coding model-detail-wins — rejected (correction #2).
- Rationale: Every pane is a proven Pi-native component (`MccOverviewList`, `SelectList`, `panelLines`, `clampLines`). The three-region layout uses the same `│` divider pattern from D49's `NavDetailPane` for visual separation. Validation: 27-check regression suite PASS (including three new D50 tests: surface responsive, detail-follows-focus, divider+three-region); strict type-check PASS; live at width ~120: surface renders CURRENT MODEL / nav+models / focus-driven detail / contextual footer; provider wording confirmed truthful; verify.py OK.

### D51 — `/model` opens the Model Control Surface directly (host dispatch override)

- Date: 2026-08-25
- Context: D50 shipped the three-region control surface but `/model` still opened the native Pi one-column model picker first; the user had to select a model before the control surface appeared. D43 proved the host hard-codes `/model` textually in `interactive-mode.js::setupEditorSubmitHandler` before extension dispatch, and the D44 bridge only patched `agent-session.js`. The user locked the requirement: `/model` must open the control surface directly, with the native picker no longer the first screen.
- Decision: Extend the version-guarded host bridge (`pi-model-bridge.mjs`) with a D51 patch to `interactive-mode.js`: before the hard-coded `/model` dispatch, check if the extension runner has a registered command named `model` (via the public `getRegisteredCommands()` API); if found, create the command context and invoke its handler. This redirects `/model` to the Harness Pi Model Control Surface. The extension registers `pi.registerCommand("model", ...)` as the unified entry point. `openModelBrowser` is retired (functionality folded into the surface). The D44 same-model bridge is preserved.
- Alternatives: upgrading Pi — rejected (0.84.3 identical); InputEvent transform — rejected (TUI slash text never reaches the session input pipeline); registering the command under a different name — rejected (the user-facing command must remain `/model`).
- Rationale: The bridge is the smallest possible delta at the exact missing dispatch point. The `getRegisteredCommands()` API is the documented extension runner surface. Validation: bridge status shows both D44 and D51 APPLIED; strict type-check PASS; 27-check regression suite PASS (incl. D50 surface tests and D48 scope tests); live `/model` at width ~120 opens the control surface directly (no native picker), with CURRENT MODEL panel, nav|models detail, and contextual footer all visible; user state (reasoning v3, settings) restored to canonical values after validation.

### D52 — Phase 2 admin-integration stance confirmed: skip (transparent fallback remains)

- Date: 2026-09-01
- Context: D42 audit §18.1 left the Phase 2 stance (9router admin access for truthful CONNECTED/ENABLED states) as the one decision blocked on the user. Options were: skip / open-local (disable require-login) / session cookie / upstream feature request for a read-only status token.
- Decision: User selected **skip for now** (2026-09-01). The Phase 1 baseline stays locked: providers render "Configured / available to Pi" + "Connectivity: Unverified"; model truth = DISCOVERED ∩ VISIBLE; no consent file is written (`~/.pi/agent/harness-router.json` remains designed-but-unused); no secrets at rest. The stance can be revisited at any time; an upstream read-only status token remains the preferred long-term answer if Phase 2 is ever resumed.
- Alternatives: open-local — rejected (weakens router posture globally: any local process reaches `/api/keys`); session cookie — rejected (secret at rest, unpredictable expiry, audit "not recommended"); upstream feature request — noted as the preferred future path, not actionable on installed 9router 0.5.x.
- Rationale: Zero new attack surface, zero code change, Phase 1 wording is already truthful. Baseline re-validated at confirmation time: verify.py OK; bridge status D44+D51 APPLIED on pi 0.83.0; deployed extension matches repo; 27-check regression suite PASS; strict type-check PASS.

### D53 — `/model` final information architecture: PROVIDERS | MODELS browser + focus-following detail + horizontal reasoning profiles

- Date: 2026-09-01
- Context: The D50 three-region layout (NAVIGATION | MODELS | DETAIL with profiles in the left nav) was explicitly rejected by the user: profiles, models, provider context and detail read as unrelated vertical columns. The user approved a final architecture: PROVIDERS left, MODELS right, SELECTED MODEL details and REASONING PROFILES as dedicated lower regions, contextual footer — all on the single `/model` screen, profiles horizontally dense.
- Decision: Rewrite `ModelControlSurface` composition only — catalog, reasoning v3, guards, bridge and state semantics untouched. New regions: two-pane PROVIDERS | MODELS browser (wide ≥ 64, stacked below); detail panel follows focus (PROVIDER → SELECTED MODEL → PROFILE — never model-dominant); `ReasoningProfilesPanel` renders all configured profiles as wrapped horizontal chips with distinct markers (› focus, ★ default, ● execution, capability-gated rows dimmed and skipped); model rows drop the provider prefix (pane scope already names it; display names with canonical id as description); search lives inside the model pane with a visible `> query` line; footer text is contextual per focused region. `MccOverviewList` survives as the provider-pane renderer; `PROFILE_GROUPS` and `mccProfileValue` retired. `ModelSurfaceState` persists focus, provider scope, search filter and profile cursor across the `/model` loop so selections and edits return to the same view. Model-switch safety (D37 approval, no silent switching) and D44/D51 bridge behavior unchanged.
- Alternatives: keeping three-region with restyled panes — rejected (composition itself was rejected); moving profiles into a modal list — rejected (profiles must be visible before editing); vertical profile cards — rejected (user wants all ten visible without scrolling); a second command — rejected (/model remains the sole primary entry).
- Rationale: The rejected layout conflated two independent dimensions (reasoning profiles, provider scope) in one nav list; the final one separates them and gives each a natural geometry. Validation: 32-check regression suite PASS (9 new D53 architecture tests: two-pane+lower regions at widths 8–400, provider truthfulness + never-selectable unconfigured rows, scope via Enter, in-scope search with visible filter line, focus-following detail, structured model metadata, provider-prefix-free rows, contextual footer, abutment/overflow kept); strict type-check PASS; verify.py OK; live TUI battery: first screen shows all five regions without input, provider Enter scopes the list, model navigation updates SELECTED MODEL, profile edit Vision Medium→Low reflects instantly across chip+detail+state with model untouched, same-model Enter re-renders in place (no dead-end), single Esc closes. One live-session defect found and fixed: `model_select` fired by the surface's own `setModel` stacked a second loop over the open one (double-Esc, state carryover) — absorbed via `modelSurfaceLoops` guard; re-validated live.
### D54 — `/model` single-active-focus navigation polish

- Date: 2026-09-01
- Context: D53 shipped the final IA but passive regions still rendered their tracked selection with a competing cursor (› on provider + model + profile simultaneously), leaving "where is my keyboard focus?" ambiguous. Root constraint discovered: pi-tui's `SelectList.renderItem` hard-codes the `→ ` selection prefix and ignores `selectedPrefix`, so a theme-closure fix is impossible.
- Decision: One region carries the keyboard cursor at any time. `MccOverviewList` and `ReasoningProfilesPanel` gained a `showCursor` gate (passive rendering keeps rows readable: no `›`, no bounded highlight, state markers ✓/★/● intact); the models pane moved from `SelectList` to the focus-gated `MccOverviewList` (identical navigation/viewport semantics, cursor controllable); `ModelControlSurface.render` sets all three gates from `persistent.focus`; search prompt renamed to `Search: <query>`; footers now say `←→ Switch Region` / `← Providers` / `← Models` per focus.
- Alternatives: patching SelectList via theme — impossible (prefix hard-coded); overlay-stripping ANSI after render — rejected (fragile, fights the renderer); dimming passive regions heavily — rejected (regions must stay readable, only the cursor differentiates).

### D57 — 9router startup is MANUAL; catalog refresh follows router readiness

- Date: 2026-09-01
- Context: D56's diagnosis proved that `/model` classified 9router "registered · not configured" with an empty browser whenever the router was down at Pi boot, because the extension only refreshed the catalog on the health-ok branch and Pi never re-evaluates the availability snapshot per render. The initial D56 fix (refresh after a successful `autoStart9router`) was superseded mid-implementation by an explicit user decision: **9router must never be started by Pi.** (During validation it also became evident that 9router's own tray wrapper auto-restarts its server child and users run it interactively — any Pi-side spawning would fight the user's ownership of the process.)
- Decision: Remove `autoStart9router` and all spawning from the extension. Startup flow: health ok → one catalog refresh (`refreshCatalogWhenRouterReady`) + boot-default restore handshake; health fails → status "offline" + one warning notice ("Start it manually, then open /model"), nothing spawned, no retries, no polling. Recovery path for a manual mid-session start: `runModelControlSurfaceLoop` performs a bounded one-shot refresh when the availability snapshot is empty and the router is now reachable (one check per surface open; if the router is still down the surface renders the truthful offline state and the next open re-arms the gate). Offline provider rows say "not running — start 9router manually" instead of "not configured". Connectivity wording remains UNVERIFIED (D52 untouched).
- Alternatives: keep auto-start + refresh (superseded by user decision); poll health every N seconds — rejected (no-polling invariant); cache the catalog to disk for offline boot — rejected (would show stale models as selectable, violating truthful availability).
- Rationale: Router readiness and catalog freshness are now symmetric (healthy at boot → refresh; healthy later → refresh on next /model open), while process ownership stays entirely with the user. Validation: 42-check suite PASS (new D57 tests: healthy-boot refresh fires once; refresh failure absorbed; static proof that session_start contains no autoStart/spawn; recovery-gate fires exactly once per open and only on an empty snapshot; classification corrects after refresh); strict tsc PASS; verify.py OK; live: router running at boot → `● 9router 266`, boot-default restored. Live router-down boots were not executed end-to-end — validation killed user-owned 9router processes repeatedly and was halted; the down-path is covered by the static no-spawn test and code-path review.

### D59 — `/model` purple three-column IA: inspector column, aligned level grid, framed editor, bundled theme

- Date: 2026-09-03
- Context: D58's surface was structurally correct but read as CLI output: no third column, weak purple identity, reasoning levels not semantically colored, and the profile editor escaped the MCC frame. The owner approved a locked IA: Row 1 = CURRENT MODEL band + PROVIDERS | MODELS | SELECTED MODEL three-column browser (inspector = column 3), Row 2 = REASONING PROFILES aligned grid, one purple MCC frame with inline title. Constraint discovered during planning: `Theme.fg()` accepts only `ThemeColor` enum tokens — purple identity must come from a theme FILE, never raw ANSI in the extension.
- Decision: (1) `ModelControlSurface.render` builds a three-column box at innerWidth ≥ 78 (`provW = clamp(22,30, 0.22·iw)`, `selW = clamp(26,34, 0.28·iw)`, models get the remainder minus 4); column 3 is a PASSIVE inspector that always mirrors the highlighted (or current) model — focus regions stay PROVIDERS→MODELS→PROFILES, ←→ unchanged; below innerWidth 78 the box collapses to two columns with the inspector as a full-width region below the box; below innerWidth 56 the D58 stack remains. (2) Inspector shape: name / route·ctx merged / `N output` line when the Pi Model carries `maxTokens` (`AvailableModelMeta.maxTokens?`, formatted 131072→"131k", 1050000→"1.1M", absent→absent) / Capabilities / status; provider & profile detail remain only in the narrow stack via the unchanged `detailLines()` dispatcher. (3) `ReasoningProfilesPanel` renders a true two-line grid: the level cell indents by exactly the name line's prefix width (cursor slot 2 + marker width) so `● level` aligns under the profile name's first letter in every column, colored through a module-level `LEVEL_COLOR` map (Off→dim, Low→success, Medium→borderAccent, High→warning, Ultra→customMessageLabel). (4) The profile editor keeps its `ctx.ui.custom` flow but draws the SAME outer frame via a single module-level `frameLines()` (title " MODEL CONTROL CENTER " inline, borderAccent bold; strokes dim) — content: EDIT PROFILE label, profile name, description, Reasoning level line, SelectList (cursor `›`), "Enter Save   Esc Cancel"; centered as a 52-column panel with D45 clamps. (5) `mcc-purple.json` (dark.json derivative: accent #b48ead, borderAccent/customMessageLabel #9575cd, text #e6e0f0, muted #8f87a8, dim #5c5570, borders #4a4262/#3a3450, selectedBg #3d3352, warning #e0af68, thinkingLow #7fb285 / thinkingXhigh #c792ea) ships in capabilities/extensions/ and syncs to ~/.pi/agent/themes/ — Pi discovers themes ONLY from the themes dir, so executeSync gained a 3b entry mapping it there (the extensions dir is NOT theme-discoverable, correcting the plan's fallback assumption). `runModelControlCenter` applies the theme via the public extension API (`ctx.ui.getTheme("mcc-purple")` + `ctx.ui.setTheme`) on open and restores the previous theme name in `finally` on close; all failures are swallowed best-effort and the surface renders with whatever theme is active — every UI color remains an enum token, so any theme works. The standalone "MODEL CONTROL CENTER" title line and CURRENT MODEL panel in the custom factory were removed (the surface owns the whole Row 1 band inside the frame now).
- Alternatives: hardcoding ANSI purple in the extension — rejected (breaks `Theme.fg()` discipline and user-theme respect); loading the theme from the extensions dir — impossible (resource-loader scans only themes dirs); making the inspector a focusable region — rejected (§12 passive); theme-switch via host-internal `theme-controller` import — unnecessary (public ctx.ui API suffices).
- Rationale: One frame implementation serves both screens (surface + editor) so the MCC identity is visually persistent without redesigning the modal flow; the semantic LEVEL_COLOR grid removes the "Taskoff" abutment class of bug by construction (column width fits the widest cell of either line). Validation: 49-check regression suite PASS (42 existing updated for the new layout + 7 new D59 tests: T1 inspector capability gating and merged metadata, T2 output-limit present/never-fabricated, T3 three-column/two-column/stacked breakpoints with junction counts, T4 dot-under-name alignment for every chip incl. disabled handling, T5 frameLines titled rule, T6 theme file schema+palette+hex-or-var discipline, LEVEL_COLOR span test); strict tsc PASS; verify.py OK; live TUI verified (below). Known deviations from the plan text: the level-line indent is per-chip prefix width (2+marker), not a fixed 3 spaces, because T4's contract demands dot==name column; the output fragment renders on its own line rather than appended to the ctx fragment because a fully merged line cannot fit selW=34 and T2 requires "131k output" to be visible.

### D60 — `/model` precision visual correction: bounded inspector, junction-exact divider geometry, distinct reasoning palette

- Date: 2026-09-04
- Context: Post-D59 live screenshots exposed three defects against the approved IA (which stays LOCKED — no architecture change): (1) the Selected Model inspector could visually extend toward/beyond the outer MCC frame — `modelInspectorLines()` rendered against no explicit budget and the render loop's whole-row fallback truncated the trailing border `│` when an inspector line was too long; (2) both browser dividers sat one display column LEFT of their `┬`/`┴` junctions (interior rows omitted the leading rail: `${l}${lp}│${m}${mp}│${s}${sp}│` totals `innerWidth − 1`), reading as three floating widgets; (3) `LEVEL_COLOR.Medium = "borderAccent"` and `Ultra = "customMessageLabel"` both resolve to `#9575cd` in mcc-purple — Medium and Ultra were visually identical.
- Decision: (1) Bounded inspector — `modelInspectorLines(selContentW)` renders against `selW − 2` (the column's own content budget, never the terminal width) with ellipsis truncation; interior rows are now complete box rows `│ cell │ cell │ cell │` so a full row always equals the border width exactly and no content can push past a rail; inspector layout restructured to the approved target (name / route / `ctx · output` merged with "context size unknown" fallback / dim CAPABILITIES header + one `● capability` row each / status) so each fact truncates at its own line and no capability is silently dropped. (2) Junction-exact geometry — row template `│${l}${lp}│${m}${mp}│${s}${sp}│` with `modelW = innerWidth − provW − selW − 4` makes `1 + provW + 1 + modelW + 1 + selW + 1 = innerWidth` an identity; every divider occupies exactly its junction column from `┬` to `┴` on every row (same contract applied to the two-column collapse). (3) Distinct palette — `LEVEL_COLOR`: Medium → `thinkingMedium` (`#81a2be` steel blue), High → `thinkingHigh` (`#e0af68` amber), Ultra → `thinkingXhigh` (`#c792ea` violet); Off (`dim` gray) and Low (`success` green) unchanged; purple reserved for the MCC identity (accent) and Ultra alone; theme tokens only, so the in-frame editor inherits the palette.
- Alternatives: widening the inspector clamp to the terminal width — rejected (violates the column-bounds rule); adding per-column nested borders to "fix" dividers — rejected (anti-pattern; the browser stays ONE component); redefining `borderAccent`/`customMessageLabel` in the theme file — rejected (they serve the theme's own UI semantics; the thinking ladder tokens already encode the mandated palette); raw hex in the extension — rejected (D59 token discipline).
- Rationale: Row-width-as-identity removes the whole drift class by construction instead of patching offsets; the per-line capability rows fix a real information-loss bug (narrow widths truncated `Capabilities ● reasoning ● vision` past `vision`); palette tokens give Medium/Ultra immediate visual distinction at zero theme cost. Validation: 52-check regression suite PASS (49 updated + 3 new: D60 GEOMETRY junction-exact rows at widths 82–400, D60 INSPECTOR BOUNDS worst-case long spec truncates with ellipsis inside column/frame, D60 LEVEL COLORS Medium≠Ultra tokens + theme hexes); strict tsc PASS; verify.py OK; deployed copy in sync; live TUI validated on the real router catalog (all three focus states show exactly one cursor, `/model` opens the surface, editor opens/cancels with Esc, Current Model band intact); 9router never touched.

### D61 — `/model` browser divider integration: one purple-family token, one row builder

- Date: 2026-09-04
- Context: Post-D60 feedback: the browser's internal dividers rendered bright/white-looking, visually disconnected from the purple MCC frame system. Root cause: both row-assembly paths (three-column and two-column) interpolated raw unstyled `│` glyphs in template literals — unstyled glyphs render in the terminal's default foreground (near-white), while the box outline, outer frame, and outer-frame rails are `dim`-styled strokes. Geometry was already correct (D60 row-width identity); this was purely a color/ownership defect.
- Decision: (1) ONE row builder — `ModelControlSurface.browserRow(cells, widths)` is the single source of truth for browser rails and dividers: it emits the leading rail, each cell padded to its column width, and the divider/trailing rails from the SAME widths array that sizes the top/bottom dash runs, so divider x == junction x by construction; both render paths (`threeCol` and the two-column collapse) route through it; the per-row padding and template-literal assembly are deleted. (2) ONE token — the divider `│` is styled `border` (`#4a4262` in mcc-purple: deep muted purple, darker than the `dim` `#5c5570` outline, same purple family as the frame system), never white, never brighter than the frame. No geometry change, no column-width change, no token change anywhere else.
- Alternatives: `borderMuted` (#3a3450) — rejected as the primary (too close to the background; `border` keeps structure visible while staying secondary); `borderAccent`/`accent` — rejected (brighter than the frame, competes with focus); leaving `dim` on dividers — rejected (indistinguishable from the outline does not satisfy "same family, slightly muted"); hardcoding hex — rejected (ThemeColor token discipline, invariant #10).
- Rationale: Centralizing row assembly removes the last place where rail color/geometry could drift between paths, and the token choice puts the internal dividers in the frame's own purple family one step darker than the outline — "children of the parent frame". Validation: 55-check regression suite PASS (52 + 3 new D61 tests: DIVIDER COLOR token-recorded assertion that every browser rail span uses `border` and the theme resolves it to #4a4262; DIVIDER GEOMETRY divider-x==top/bottom-junction-x at widths 80–400 incl. the two-column collapse at 80; DIVIDER CONTINUITY exact rail-column arrays — outer rail, leading rail, both dividers, trailing rail, corner rail — present in EVERY interior row at widths 80–200); strict tsc PASS; verify.py OK; truecolor span count on the real theme: 32 border rails + 40 dim outer rails, 0 unstyled, 0 near-white; live TUI stable across navigation, in-pane search, same-model select, and Esc close; 9router untouched.

### D62 — Runtime Context Bar + reduced footer: runtime identity on the primary surface, diagnostics demoted

- Date: 2026-09-04
- Context: The main interaction surface read as generic CLI: the built-in footer's extension-status line (`online 🔌 MCP: 4 servers … LSP … [Generic]`) mixed infrastructure diagnostics into the primary view, while the runtime facts that matter (lifecycle, model, reasoning level, active profile, workspace, context usage) were scattered or absent. Owner approved the D62 plan: compact Runtime Context Bar above the editor, one-line custom footer, extension statuses removed from the primary surface (they remain in `/doctor`), `/model` untouched, dark-theme token-only styling, no editor changes.
- Decision: (1) `RuntimeContextBar` (new, in runtime-orchestrator.ts) hosted via `ctx.ui.setWidget("runtime-context", factory, { placement: "aboveEditor" })`: line 1 = `π` + lifecycle (`○ Ready` muted / braille `Running · MM:SS` accent / `✓ Complete · MM:SS` success / `✕ Error` error, braille frames + 80 ms interval identical to the built-in Loader) + model display name (bold, from harness-models.json names) + `● <Level>` in the D60 `LEVEL_COLOR` token + workspace (`~`-shortened, dim) + `<used> / <limit> · <P>%` (muted/warning/error at the built-in footer's >70/>90 thresholds; `? / limit` post-compaction); line 2 while running = activity phrase (Editing/Writing/Reading file, Running <cmd>, Searching <pattern>, fallback Running <tool>); otherwise `Default · <profile>` or `Execution · <profile>` (muted). Narrow-width drop order: workspace → line 2 → compact usage `<used>/<limit>` → final clamp; π+lifecycle+model never dropped by design. State is a module-level store mutated ONLY by event handlers (`agent_start`/`agent_end` stopReason scan/`agent_settled`/`tool_execution_start`/`tool_execution_end`/`session_start`/`session_shutdown`) — event-driven, zero polling; the 80 ms interval only advances the frame and repaints. (2) `ReducedFooter` via `ctx.ui.setFooter`: one line `<branch> · ↑in ↓out R<r> W<w> CH<h>% $<cost>` from `footerData.getGitBranch()` + a local usage reducer mirroring the built-in footer's aggregation; statuses intentionally not rendered. (3) RAL's three `ctx.ui.setStatus` fragments (topology, 9router online/offline, reasoning) deleted — clean cutover; router health stays visible via `/model` offline wording, `/doctor`, and `notify()`. (4) The bar hides (setWidget undefined) while the Model Control Center is open and is restored with the same factory in the wrapper's finally on every close path. Registration lives in the session_start handler guarded by `hasUI && mode === "tui"` (headless no-op); every ctx accessor in the render path is try/caught against assertActive.
- Alternatives: Keeping the built-in footer and styling it — rejected: no host patch, and status-line noise is what the owner asked to remove; a separate status script — rejected: polling; rich per-tool activity log — rejected: one activity at a time, no raw tool names; theme file changes — rejected: owner chose token-only styling under the existing dark theme; rendering statuses in the custom footer — rejected for v1 (one-line fallback exists if a signal is missed).
- Rationale: One component owns runtime identity where the eye already is (above the editor); infrastructure diagnostics keep a home (`/doctor`) without polluting every frame. The built-in footer's aggregation and threshold semantics are adopted, not invented, so numbers stay consistent across surfaces. Validation: 70-check suite PASS (55 prior + 15 new D62 checks: composition for ready/running/complete/error/percent-null, warning 82%/error 96% tones via fixtures, activity mapping table, drop order at 76/50/40, width-safety sweep 8–400 with π preserved, footer line with branch+stats and zero status keys, reducer totals + last-assistant CH rate, formatElapsed/formatTokensCompact units); strict tsc PASS; verify.py OK; live TUI captures: Ready one-line bar + reduced footer, Running spinner + elapsed + activity line, `✓ Complete · MM:SS` + profile line after settle, no MCP/LSP line at any state; BEFORE/AFTER frames at 100/140 cols archived in the session report.

### D63 — `/model` provenance fix: the D51 bridge dispatched the extension model command on EVERY submission

- Date: 2026-09-04
- Context: Owner report: typing any normal message ("testing") + Enter unexpectedly opened the Model Control Surface; D62 live validation had also recorded an "~20 s after startup, /model opens" symptom. Evidence gathered under instrumentation: (a) the host's `_emitModelSelect` fired ZERO times during a repro run — the `model_select`/D44 path was NOT the trigger and the `restoringBootDefault` guard held; (b) a stack probe inside `runModelControlCenter` captured the real path: `Object.handler (runtime-orchestrator.ts:3428) <= defaultEditor.onSubmit (interactive-mode.js:2127) <= CustomEditor.submitValue`. Root cause: the D51 patch blob in `pi-model-bridge.mjs` inserted the extension-dispatch block BEFORE the builtin `if (text === "/model" …)` guard, so `onSubmit` executed `_modelCmd.handler(text…)` for EVERY submitted line — normal text included. The extension `/model` handler ignores its argument, so any submission opened the MCC. The "~20 s" startup symptom was the same defect: it appeared whenever input (including automated capture prompts) was submitted during the boot window; a no-input boot never auto-opened (verified over 30 s).
- Decision: Fix the D51 patch blob so the extension dispatch sits INSIDE the `/model` text guard: `if (text === "/model" || startsWith("/model ")) { …extension dispatch… }` followed by the re-emitted builtin guard for the no-extension fallback. `pi-model-bridge.mjs` gains `D51_LEGACY_PATCHED` (the defective blob shape) and `apply()` now migrates hosts carrying it before applying the corrected blob (idempotent re-apply path). No `model_select` semantics changed; D44 same-model emission, `shouldOpenControlCenter`, `restoringBootDefault`, and D57 manual-start are untouched.
- Alternatives: Provenance field on `model_select` (source="user"/"programmatic") — rejected: the event was never the trigger; adding a provenance API would fix nothing and churn the host patch. Ignoring all `model_select` events or a global suppress flag — rejected per owner instruction (would break D44). Time-delay suppression — rejected: hides the race instead of fixing dispatch. Reverting the bridge — rejected: Pi 0.83.x still needs the same-model bridge and D51 direct entry.
- Rationale: The bug was never "the model UI opens too easily" but "programmatic-looking dispatch ran on user text" — the D51 blob executed an unconditional handler call where a guarded one was intended. Moving the dispatch inside the guard restores exact builtin semantics for non-`/model` text (native submission, no interception) while preserving the extension override for genuine `/model`. Validation: real-TUI battery on the patched host — boot with no input (startup + catalog refresh + restoration window): MCC never opens; `testing`/`hello world`/`model test` + Enter: normal flow, no MCC; empty Enter: no-op; `/model` + Enter: MCC opens; Esc closes back to the editor. 77-check suite PASS (55 D42–D62 + 7 new D63 checks: fixed-blob guard ordering, legacy-blob defect shape pinned, submit simulation for normal text, explicit `/model` with args, builtin fallback body preserved in the installed host, migration logic present, D44/version guards unchanged); strict tsc PASS; verify.py OK.

### D64 — Main runtime context + input surface visual redesign: activity / context / input layers

- Date: 2026-09-04
- Context: D62's runtime bar was functionally correct but visually generic: lifecycle, model, reasoning, profile, workspace and usage sat in one 1–2-line text dump directly above the editor, the native `Working...` spinner duplicated the custom running indicator, third-party status widgets (pi-lens) leaked diagnostics back into the primary surface, and the boxed host editor read as generic CLI.
- Decision: (1) THREE-LAYER composition. LAYER 1 ACTIVITY (transient): dedicated above-editor widget rendering ONLY while running — `◐ MM:SS · <phrase>` (animated circle frames ◐◓◑◒ at the Loader's 80 ms cadence, event-driven, zero polling), settling to `✓ Complete · MM:SS` / `✕ Error`, and rendering ZERO lines when idle (no reserved vertical space). LAYER 2 CONTEXT BAR (persistent): one OMP-style information spine — `<model> · ● <level> · <profile> │ 📁 <workspace> │ <used>/<limit> · <p>%` — with a dim rail between groups, a middot inside the model·reasoning·profile group, and the locked width-drop order: workspace → profile suffix → usage compacted → usage dropped → final clamp (model and ● level never drop). LAYER 3 INPUT SURFACE: the host editor is replaced through the PUBLIC setEditorComponent API with a lazily-built CustomEditor subclass whose render() strips the generic top/bottom rules and prefixes content with an accent, bold `π │` gutter — open, frameless, OMP-style; all editor behavior (multiline, cursor, autocomplete, keybindings, paste) inherited unchanged.
- Alternatives: Keeping lifecycle text in the bar — rejected: merged the transient into the persistent (the exact 'bad' shape the owner rejected). Boxing the context bar — rejected: dashboard feel; a one-line spine reads cleaner. Host patch for the editor gutter — rejected: setEditorComponent is a public API designed for exactly this. Polling/repeated suppression timers for pi-lens — rejected beyond two bounded one-shots; the persistent fix is pi-lens' own widget.visible=false global config, documented for the owner.
- Rationale: The three layers now have distinct visual roles (transient explanation / persistent identity / interaction) and cannot collapse into one text dump. The native Working... duplication is removed through the public setWorkingMessage('') + setWorkingIndicator({frames: []}) APIs — no host patch. pi-lens' below-editor widget is suppressed via two bounded one-shot re-asserts after its own session_start mount, and the persistent clean state comes from pi-lens' native widget.visible=false config. Validation: 78-check suite PASS (8 new D64 checks: activity line states, widget zero-line idle, OMP spine composition, null-usage segment, drop order at 90/60/40/20, 8–400 width sweep, bar class, tokens); strict tsc PASS; verify.py OK; real-TUI captures: READY (clean spine, π gutter, folder icon, no pi-lens, no Working...), RUNNING (single animated activity line + spine + updated usage), COMPLETE (✓ settle). Model/profile independence, D44/D51/D57/D63, reasoning v3 and width safety untouched.

- Amendment (2026-09-04, same session — owner-locked final composition): the context bar became a **runtime-context FIELD** — a rounded frame (`╭── … ──╮` / `╰────╯`) whose top rule carries the information spine inline and embeds the context usage at the right (`── <p>% ┃ <limit> ──╮`), with `╭── ` padding keeping information off the border. The spine gained the approved identity: lifecycle dot (○ muted / ◉ accent while running), \`★ <profile>\` (accent star, muted name), \`> 📁 <workspace>\`, \`> ⑂ <branch>\` (branch sourced from the footer factory's FooterDataProvider — the only public git path). Drop order per the locked spec: branch → workspace → profile suffix → usage limit → usage → clamp. The input surface became a rounded box: the π identity sits ON the frame's top rule (`╭─ π │ ───╮`, accent glyph + border-styled rail via the host's own borderColor styler), content lines are bordered with one column of interior padding, and the base editor lays out at `width − 4` so side chrome never violates D45. Zero-length activity still renders zero lines; all D64 tests rewritten for the field/frame shapes (77-check suite, tsc, verify.py, real-TUI READY/RUNNING/COMPLETE all green).
